<!doctype html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="theme-color" content="#214BE8">
  <meta name="description" content="Análises de alto nível sobre canal indireto, força de vendas, sell-out, incentivos e inteligência artificial, por WMW Systems e Hub 360.">
  <title>Hub 360 — Inteligência para canais indiretos | WMW Systems</title>
  <style>
    :root {
      --blue:#214BE8;
      --night:#101C42;
      --turq:#25CEC6;
      --white:#fff;
      --ink:#111A43;
      --paper:#F6F8FF;
      --line:rgba(255,255,255,.16);
      --soft:rgba(255,255,255,.68);
      --display:"Poppins","Avenir Next","Segoe UI",Arial,sans-serif;
      --body:"Google Sans","Segoe UI",Arial,sans-serif;
      --rail:318px;
    }
    *{box-sizing:border-box}
    html{scroll-behavior:smooth}
    body{margin:0;background:var(--blue);color:var(--white);font-family:var(--body);font-size:16px;line-height:1.6;overflow-x:hidden}
    h1,h2,h3,h4,h5,h6{font-family:var(--display)}
    button,input{font:inherit}
    button,a{color:inherit}
    a{text-decoration:none}
    button{cursor:pointer}
    :focus-visible{outline:3px solid var(--turq);outline-offset:4px}
    ::selection{background:var(--turq);color:var(--night)}
    .skip{position:fixed;left:20px;top:-60px;background:#fff;color:var(--ink);padding:10px 16px;z-index:999;border-radius:4px}
    .skip:focus{top:16px}
    .progress{position:fixed;z-index:500;left:0;top:0;height:4px;width:0;background:var(--turq);box-shadow:0 0 18px rgba(37,206,198,.85);transition:width .08s linear}

    .site-header{height:88px;display:flex;align-items:center;justify-content:space-between;padding:0 44px;position:absolute;left:0;right:var(--rail);top:0;z-index:60;border-bottom:1px solid rgba(255,255,255,.06)}
    .brand{display:inline-flex;gap:13px;align-items:center;font:700 21px/1 var(--display);letter-spacing:-.02em}
    .brand-mark{display:inline-grid;gap:3px;transform:skewX(-18deg)}
    .brand-mark i{display:block;width:22px;height:5px;background:var(--white);border-radius:1px}
    .brand-mark i:nth-child(2){margin-left:5px;background:var(--turq)}
    .brand-mark i:nth-child(3){margin-left:10px}
    .nav{display:flex;align-items:center;gap:32px;font-size:14px}
    .nav a{opacity:.82;transition:opacity .2s,color .2s}
    .nav a:hover{opacity:1;color:var(--turq)}
    .nav .cta{border:1px solid var(--turq);padding:11px 18px;border-radius:5px;opacity:1}
    .menu-btn{display:none;border:1px solid var(--line);background:transparent;color:#fff;width:44px;height:44px;border-radius:5px}

    .home{min-height:100vh;background:
      radial-gradient(circle at 70% 28%,rgba(37,206,198,.10),transparent 26%),
      radial-gradient(circle at 18% 18%,rgba(255,255,255,.10),transparent 30%),
      var(--blue)}
    .hero{position:relative;min-height:650px;padding:148px calc(var(--rail) + 78px) 76px 44px;overflow:hidden;border-bottom:1px solid var(--line)}
    .hero-copy{position:relative;z-index:4;max-width:850px}
    .kicker,.meta{font-family:var(--display);font-size:12px;font-weight:700;letter-spacing:.16em;text-transform:uppercase;color:var(--turq)}
    .hero h1{font:700 clamp(52px,6.2vw,98px)/.98 var(--display);letter-spacing:-.055em;margin:22px 0 24px;max-width:1000px;text-wrap:balance}
    .hero-dek{font-size:clamp(19px,1.7vw,25px);line-height:1.55;max-width:720px;color:rgba(255,255,255,.86);margin:0;padding-left:28px;border-left:3px solid var(--turq)}
    .route-art{position:absolute;inset:64px var(--rail) 0 42%;opacity:.9;pointer-events:none}
    .route-art svg{width:100%;height:100%}
    .topo{fill:none;stroke:rgba(255,255,255,.085);stroke-width:1.2}
    .route{fill:none;stroke:var(--turq);stroke-width:5;stroke-linecap:round;stroke-dasharray:12 12;filter:drop-shadow(0 0 7px rgba(37,206,198,.4))}
    .route-ghost{fill:none;stroke:rgba(255,255,255,.5);stroke-width:1.6;stroke-dasharray:5 9}

    .edition-rail{position:fixed;z-index:55;right:0;top:0;width:var(--rail);height:100vh;background:linear-gradient(180deg,rgba(16,28,66,.52),rgba(16,28,66,.76));border-left:1px solid rgba(255,255,255,.18);backdrop-filter:blur(18px);padding:34px 34px 30px;display:flex;flex-direction:column}
    .rail-top{display:flex;justify-content:space-between;align-items:center;padding-bottom:24px;border-bottom:1px solid var(--line)}
    .rail-label{font:700 10px/1 var(--display);color:var(--turq);letter-spacing:.16em;text-transform:uppercase}
    .rail-count{font:600 12px/1 var(--display);color:rgba(255,255,255,.55)}
    .rail-items{margin:24px 0 0;padding:0;list-style:none;position:relative;overflow:auto;scrollbar-width:none}
    .rail-items:before{content:"";position:absolute;left:32px;top:16px;bottom:16px;width:1px;background:rgba(255,255,255,.2)}
    .rail-items li{position:relative;margin:0 0 4px}
    .rail-link{display:grid;grid-template-columns:48px 1fr;gap:12px;width:100%;border:0;background:transparent;text-align:left;padding:9px 0;color:#fff;opacity:.48;transition:.2s}
    .rail-link:hover,.rail-link.active{opacity:1}
    .rail-link.active{color:var(--turq)}
    .rail-num{font:700 22px/1 var(--display);letter-spacing:-.04em;position:relative}
    .rail-num:after{content:"";position:absolute;right:-19px;top:8px;width:7px;height:7px;background:rgba(255,255,255,.62);border-radius:50%;box-shadow:0 0 0 4px rgba(255,255,255,.05)}
    .rail-link.active .rail-num:after{background:var(--turq)}
    .rail-title{font-size:11px;line-height:1.45;padding-top:1px;color:inherit}
    .rail-foot{margin-top:auto;padding-top:18px;border-top:1px solid var(--line);font-size:11px;color:rgba(255,255,255,.48)}

    .library{padding:56px calc(var(--rail) + 38px) 92px 44px;background:var(--blue);min-height:750px}
    .section-head{display:flex;align-items:end;justify-content:space-between;gap:30px;margin-bottom:35px}
    .section-head h2{font:700 clamp(34px,4vw,56px)/1.05 var(--display);letter-spacing:-.04em;margin:0;max-width:690px}
    .section-head p{max-width:420px;margin:0;color:var(--soft);font-size:15px}
    .tools{display:flex;align-items:center;gap:18px;padding:14px 0;border-top:1px solid var(--line);border-bottom:1px solid var(--line);margin-bottom:20px}
    .search{flex:1;display:flex;gap:12px;align-items:center}
    .search svg{width:18px;opacity:.7}
    .search input{width:100%;border:0;background:transparent;color:#fff;outline:0;padding:6px 0}
    .search input::placeholder{color:rgba(255,255,255,.52)}
    .filter-toggle{border:0;background:transparent;color:#fff;opacity:.78;padding:8px}
    .filters{display:flex;gap:8px;flex-wrap:wrap;margin:0 0 28px}
    .filter{border:1px solid var(--line);background:transparent;color:rgba(255,255,255,.72);border-radius:999px;padding:7px 13px;font-size:12px}
    .filter:hover,.filter.active{background:var(--turq);border-color:var(--turq);color:var(--night)}
    .article-list{border-top:1px solid var(--line)}
    .story{display:grid;grid-template-columns:70px 1.2fr .65fr 30px;gap:24px;align-items:center;padding:25px 0;border-bottom:1px solid var(--line);transition:padding .2s,background .2s}
    .story:hover{padding-left:12px;background:linear-gradient(90deg,rgba(37,206,198,.08),transparent 70%)}
    .story.featured{min-height:220px;padding:36px 24px;background:rgba(9,32,115,.34);border:1px solid rgba(255,255,255,.2);margin:18px 0 8px;position:relative;overflow:hidden}
    .story.featured:after{content:"";position:absolute;inset:0 0 0 45%;background:repeating-radial-gradient(ellipse at 70% 70%,transparent 0 21px,rgba(37,206,198,.09) 22px 23px);pointer-events:none}
    .story-num{font:600 28px/1 var(--display);color:var(--turq);align-self:start;padding-top:6px}
    .story-main{position:relative;z-index:1}
    .story h3{font:600 clamp(22px,2.25vw,34px)/1.18 var(--display);letter-spacing:-.035em;margin:5px 0 8px;text-wrap:balance}
    .story.featured h3{font-size:clamp(31px,3.1vw,48px);max-width:720px}
    .story-summary{font-size:14px;color:var(--soft);line-height:1.55;position:relative;z-index:1}
    .story-arrow{font-size:25px;color:var(--turq);transition:transform .2s;position:relative;z-index:1}
    .story:hover .story-arrow{transform:translateX(6px)}
    .empty{padding:60px 0;color:var(--soft);font-size:18px}

    .about{padding:110px calc(var(--rail) + 80px) 115px 44px;background:var(--turq);color:var(--night);display:grid;grid-template-columns:.7fr 1.3fr;gap:70px}
    .about .kicker{color:var(--blue)}
    .about h2{font:700 clamp(42px,5.4vw,78px)/.98 var(--display);letter-spacing:-.055em;margin:16px 0 0}
    .about-copy{font-size:20px;line-height:1.6;max-width:680px}
    .about-copy p{margin:0 0 24px}
    .dark-btn{display:inline-flex;align-items:center;gap:18px;background:var(--night);color:#fff;padding:15px 21px;border-radius:5px;font-weight:700;font-family:var(--display);font-size:14px}
    .site-footer{padding:35px calc(var(--rail) + 40px) 35px 44px;background:var(--night);display:flex;justify-content:space-between;color:rgba(255,255,255,.55);font-size:12px}

    .reader{display:none;background:var(--paper);color:var(--ink);min-height:100vh}
    body.reading .home{display:none}
    body.reading .reader{display:block}
    body.reading .edition-rail{display:none}
    body.reading .site-header{position:absolute;right:0}
    .article-hero{background:
      radial-gradient(circle at 70% 20%,rgba(37,206,198,.13),transparent 28%),
      var(--blue);color:#fff;padding:134px 44px 74px;min-height:610px;position:relative;overflow:hidden}
    .article-hero-inner{max-width:1420px;margin:auto;position:relative;z-index:2}
    .back{display:inline-flex;align-items:center;gap:9px;color:var(--turq);font-size:14px;margin-bottom:30px}
    .article-hero h1{font:700 clamp(46px,5.7vw,86px)/1.02 var(--display);letter-spacing:-.055em;max-width:1120px;margin:15px 0 22px;text-wrap:balance}
    .article-dek{font-size:clamp(18px,1.6vw,24px);line-height:1.48;max-width:820px;color:rgba(255,255,255,.82);margin:0 0 34px}
    .byline{display:flex;gap:26px;align-items:center;font-size:13px;text-transform:uppercase;letter-spacing:.06em;color:rgba(255,255,255,.7)}
    .byline span+span{padding-left:26px;border-left:1px solid rgba(255,255,255,.3)}
    .article-route{position:absolute;right:-40px;top:70px;width:50%;height:500px;opacity:.6}
    .article-route svg{width:100%;height:100%}

    .reading-layout{display:grid;grid-template-columns:280px minmax(0,790px) 240px;gap:54px;max-width:1410px;margin:0 auto;padding:76px 44px 100px;align-items:start}
    .toc{position:sticky;top:38px;background:var(--blue);color:#fff;padding:28px 24px;box-shadow:0 14px 45px rgba(33,75,232,.18)}
    .toc-title{font:700 12px/1 var(--display);letter-spacing:.13em;text-transform:uppercase;color:var(--turq);margin-bottom:18px}
    .toc ol{list-style:none;margin:0;padding:0;border-left:1px dashed rgba(37,206,198,.65)}
    .toc li{counter-increment:toc}
    .toc a{display:grid;grid-template-columns:30px 1fr;gap:7px;padding:9px 0 9px 14px;font-size:12p